# Python / backend
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.venv/
venv/

# Env files
.env
.env.*

# 跨天去重：本地 URL 历史（可按需提交到仓库以在多机共享）
.briefing_url_history.json

# Daily outputs (根据需要选择是否提交历史，这里默认提交 daily_news，所以不忽略)
# daily_news/

# Frontend (Vite + React)
web/node_modules/
web/dist/
web/.cache/
web/.vite/

# Editor / OS
.DS_Store
*.log

